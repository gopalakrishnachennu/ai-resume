# AI Resume Builder - LLM Black Box Architecture Plan

## 🎯 Core Concept: Centralized LLM Pipeline

All AI operations route through a **single LLM Black Box** that contains:
- Prompts & Instructions (organized by purpose)
- Token optimization logic
- Caching mechanism
- Provider-agnostic interface

---

## 📊 System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    LLM BLACK BOX (Central Hub)                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Prompt Library (Organized by Function)                   │  │
│  │  ├── /prompts/job-analysis.txt                           │  │
│  │  ├── /prompts/resume-generation.txt                      │  │
│  │  ├── /prompts/ats-optimization.txt                       │  │
│  │  ├── /prompts/section-suggestions.txt                    │  │
│  │  └── /prompts/improvement-tips.txt                       │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Cache Layer (Reduce API Calls)                          │  │
│  │  - Job Analysis Cache (24h TTL)                          │  │
│  │  - Suggestions Cache (per resume)                        │  │
│  │  - ATS Score Cache (per version)                         │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Provider Router (Gemini/OpenAI/Claude)                  │  │
│  │  - Auto-selects based on user config                     │  │
│  │  - Handles API calls uniformly                           │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │  LLM Provider   │
                    │ (User's API Key)│
                    └─────────────────┘
```

---

## 🔄 Complete User Flow

```
Step 1: JOB DESCRIPTION ANALYSIS
┌──────────────────┐
│ User pastes JD   │
└────────┬─────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ LLM Black Box                           │
│ ├─ Load: job-analysis.txt               │
│ ├─ Check Cache: hash(jobDescription)    │
│ │  └─ HIT? Return cached                │
│ │  └─ MISS? Call LLM                    │
│ ├─ Extract:                             │
│ │  • Job Title                          │
│ │  • Company                            │
│ │  • Keywords (technical/soft/tools)    │
│ │  • Requirements (must/nice-to-have)   │
│ │  • Years of experience                │
│ └─ Cache result (24h)                   │
└────────┬────────────────────────────────┘
         │
         ▼
┌──────────────────┐
│ Analysis Ready   │
└──────────────────┘

═══════════════════════════════════════════

Step 2: RESUME GENERATION (FAST PROCESSING)
┌──────────────────────────────────┐
│ User clicks "Generate Resume"    │
└────────┬─────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ LLM Black Box (Parallel Processing)    │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ Thread 1: Summary Generation        │ │
│ │ ├─ Load: resume-generation.txt      │ │
│ │ ├─ Input: Job Analysis + User Data  │ │
│ │ └─ Output: Tailored Summary         │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ Thread 2: Experience Optimization   │ │
│ │ ├─ Load: resume-generation.txt      │ │
│ │ ├─ Match keywords to experience     │ │
│ │ └─ Output: Optimized bullets        │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ Thread 3: Skills Matching           │ │
│ │ ├─ Load: resume-generation.txt      │ │
│ │ ├─ Prioritize relevant skills       │ │
│ │ └─ Output: Ordered skill list       │ │
│ └─────────────────────────────────────┘ │
└────────┬────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Resume populated in Editor       │
└──────────────────────────────────┘

═══════════════════════════════════════════

Step 3: ATS SCORING (PARALLEL)
┌──────────────────────────────────┐
│ While user reviews resume...     │
└────────┬─────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ LLM Black Box (Background)              │
│ ├─ Load: ats-optimization.txt           │
│ ├─ Check Cache: hash(resumeContent)     │
│ ├─ Analyze:                             │
│ │  • Keyword density                    │
│ │  • ATS-friendly formatting            │
│ │  • Missing critical keywords          │
│ │  • Section completeness               │
│ ├─ Calculate Score (0-100%)             │
│ └─ Cache result                         │
└────────┬────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Display: ATS Score Badge         │
│ (Updates in real-time)           │
└──────────────────────────────────┘

═══════════════════════════════════════════

Step 4: SMART SUGGESTIONS (If Score < 90%)
┌──────────────────────────────────┐
│ ATS Score = 75% (< 90%)          │
└────────┬─────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ LLM Black Box                           │
│ ├─ Load: section-suggestions.txt        │
│ ├─ Identify weak sections:              │
│ │  • Summary: Missing keywords          │
│ │  • Skills: Add "Docker, Kubernetes"   │
│ │  • Experience: Quantify achievements  │
│ ├─ Generate specific suggestions        │
│ └─ Cache per section                    │
└────────┬────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────┐
│ Editor: Inline Suggestions               │
│                                          │
│ ┌──────────────────────────────────────┐ │
│ │ 📝 Professional Summary              │ │
│ │ [Your current summary...]            │ │
│ │                                      │ │
│ │ 💡 Suggestion (Click to apply):      │ │
│ │ "Add keywords: Cloud Architecture,   │ │
│ │  Microservices, DevOps"              │ │
│ │ [✓ Apply] [✗ Dismiss]                │ │
│ └──────────────────────────────────────┘ │
│                                          │
│ ┌──────────────────────────────────────┐ │
│ │ 🛠️ Technical Skills                  │ │
│ │ [Your current skills...]             │ │
│ │                                      │ │
│ │ 💡 Suggestion (Click to apply):      │ │
│ │ "Add: Docker, Kubernetes, Terraform" │ │
│ │ [✓ Apply] [✗ Dismiss]                │ │
│ └──────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

---

## 📁 Folder Structure

```
/lib/llm-black-box/
├── prompts/
│   ├── job-analysis.txt           # Extract job requirements
│   ├── resume-generation.txt      # Generate resume sections
│   ├── ats-optimization.txt       # Score and analyze ATS compatibility
│   ├── section-suggestions.txt    # Improve specific sections
│   └── improvement-tips.txt       # General optimization tips
│
├── cache/
│   └── cacheManager.ts            # Handle caching logic
│
├── providers/
│   ├── geminiProvider.ts          # Gemini API wrapper
│   ├── openaiProvider.ts          # OpenAI API wrapper
│   └── claudeProvider.ts          # Claude API wrapper
│
├── core/
│   ├── llmRouter.ts               # Route to correct provider
│   ├── promptLoader.ts            # Load prompts from files
│   └── tokenOptimizer.ts          # Minimize token usage
│
└── index.ts                       # Main Black Box interface
```

---

## 🚀 Token Optimization Strategies

### 1. **Caching Strategy**
```
┌─────────────────────────────────────┐
│ Cache Key Generation                │
├─────────────────────────────────────┤
│ Job Analysis:                       │
│  key = hash(jobDescription)         │
│  TTL = 24 hours                     │
│                                     │
│ Resume Generation:                  │
│  key = hash(jobAnalysis + userData) │
│  TTL = Until user modifies          │
│                                     │
│ ATS Score:                          │
│  key = hash(resumeContent)          │
│  TTL = Until content changes        │
│                                     │
│ Suggestions:                        │
│  key = hash(section + atsScore)     │
│  TTL = Until section changes        │
└─────────────────────────────────────┘
```

### 2. **Smart Chunking**
- Only send relevant data to LLM
- Job description: Max 1000 chars (summarize if longer)
- Experience: Only last 3 positions
- Skills: Top 20 most relevant

### 3. **Batch Processing**
- Generate all sections in ONE call when possible
- Use structured JSON output
- Avoid multiple round-trips

### 4. **Incremental Updates**
- Only re-analyze changed sections
- Don't regenerate entire resume

---

## 🎨 UI/UX Flow

```
┌────────────────────────────────────────────────────────────┐
│  STEP 1: Job Description                                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ [Paste Job Description]                              │  │
│  │                                                      │  │
│  │ [Analyze Job] ← Fast (cached if seen before)        │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
                         ↓
┌────────────────────────────────────────────────────────────┐
│  STEP 2: Analysis Preview                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 📊 Job Analysis                                      │  │
│  │ • Title: Senior DevOps Engineer                      │  │
│  │ • Company: Tech Corp                                 │  │
│  │ • Key Skills: AWS, Docker, Kubernetes                │  │
│  │                                                      │  │
│  │ [Generate Resume] ← FAST PROCESSING                  │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
                         ↓
┌────────────────────────────────────────────────────────────┐
│  STEP 3: Resume Editor (Live Preview)                      │
│  ┌─────────────────────┬────────────────────────────────┐  │
│  │ ✏️ Edit             │ 👁️ Preview                     │  │
│  │                     │                                │  │
│  │ [Summary]           │ ┌────────────────────────────┐ │  │
│  │ [Experience]        │ │ Your Resume Preview        │ │  │
│  │ [Skills]            │ │ ...                        │ │  │
│  │ [Education]         │ └────────────────────────────┘ │  │
│  │                     │                                │  │
│  │                     │ 📊 ATS Score: 75%              │  │
│  │                     │ ⚠️ Below 90% - See suggestions │  │
│  └─────────────────────┴────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
                         ↓
┌────────────────────────────────────────────────────────────┐
│  STEP 4: Smart Suggestions (Inline)                        │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 📝 Professional Summary                              │  │
│  │ ┌──────────────────────────────────────────────────┐ │  │
│  │ │ Experienced professional with...                 │ │  │
│  │ └──────────────────────────────────────────────────┘ │  │
│  │                                                      │  │
│  │ 💡 AI Suggestion:                                    │  │
│  │ ┌──────────────────────────────────────────────────┐ │  │
│  │ │ Add these keywords to boost ATS score:           │ │  │
│  │ │ "Cloud Architecture, CI/CD, Infrastructure"      │ │  │
│  │ │                                                  │ │  │
│  │ │ [✓ Apply Suggestion] [✗ Dismiss] [🔄 Regenerate] │ │  │
│  │ └──────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
```

---

## 🔧 Technical Implementation Plan

### Phase 1: LLM Black Box Core
1. Create folder structure
2. Write prompt templates
3. Build prompt loader
4. Implement cache manager
5. Create provider router

### Phase 2: Job Analysis Pipeline
1. Job description parser
2. Keyword extraction
3. Requirement analysis
4. Cache integration

### Phase 3: Resume Generation
1. Section generators (parallel)
2. Keyword matching
3. Content optimization
4. Fast processing mode

### Phase 4: ATS Scoring
1. Background scoring
2. Real-time updates
3. Score breakdown
4. Cache optimization

### Phase 5: Smart Suggestions
1. Section-specific analysis
2. Inline suggestion UI
3. One-click apply
4. Regeneration option

---

## 💾 Caching Strategy Details

```typescript
interface CacheEntry {
    key: string;
    value: any;
    timestamp: number;
    ttl: number; // milliseconds
    tags: string[]; // For invalidation
}

// Example Cache Keys:
// job-analysis:{hash}
// resume-gen:{userId}:{jobHash}
// ats-score:{resumeHash}
// suggestions:{sectionId}:{resumeHash}
```

### Cache Invalidation Rules:
- **Job Analysis**: 24 hours OR user clicks "Re-analyze"
- **Resume**: When user edits ANY section
- **ATS Score**: When resume content changes
- **Suggestions**: When section content changes OR user dismisses

---

## 🎯 Success Metrics

1. **Speed**: Resume generation < 5 seconds
2. **Cost**: < 5000 tokens per resume
3. **Accuracy**: ATS score within 5% of actual
4. **Cache Hit Rate**: > 60% for job analysis
5. **User Satisfaction**: Suggestions applied > 70%

---

## 🔄 Next Steps

1. **Review this architecture** - Does it match your vision?
2. **Share your layout plan** - I'll integrate it
3. **Prioritize features** - What to build first?
4. **Refine prompts** - What should each prompt focus on?
5. **Define data models** - What data flows between steps?

---

## ❓ Questions for You

1. Should suggestions be **automatic** or **on-demand**?
2. How many suggestions per section (1, 3, 5)?
3. Should we show **why** a suggestion helps ATS score?
4. Do you want **A/B testing** of different prompts?
5. Should cache be **localStorage** or **Firebase**?

---

**Ready to review! Share your layout ideas and let's refine this together!** 🚀
